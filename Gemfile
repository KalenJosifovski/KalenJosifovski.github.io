# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Add missing dependencies for Ruby 3.4+ compatibility and asset management
gem 'bigdecimal'
gem 'logger'
gem 'jekyll-assets'  # Optional plugin to manage assets more efficiently